package CurrencyInterfaceMethod;

public interface ConverterInterface {
    double rupees(double amount);
    double dollar(double amount);
    double euro(double amount);
    double pound(double amount);
}
